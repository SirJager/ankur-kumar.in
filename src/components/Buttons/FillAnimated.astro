---
interface Props {
	class_?: string;
}

const {class_ = ""} = Astro.props;
---

<div
	aria-label="change theme"
	class:list={[
		"group/tgthm relative overflow-clip px-5 py-3 text-center",
		"text-base-content hover:text-base-200",
		"transition-all duration-300 ease-in-out",
		"border-base-200 border-2",
		"after:transition-all after:duration-300 after:ease-in-out",
		"after:bg-base-content after:absolute after:h-full after:w-full after:content-['']",
		"after:-bottom-full after:left-0 hover:after:bottom-0",
		"rounded-btn after:rounded-btn",
		class_,
	]}
>
	<span class="relative z-[1]">
		<slot>Button</slot>
	</span>
</div>
